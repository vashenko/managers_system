import { ArrayConvertorPipe } from './array-convertor.pipe';

describe('ArrayConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
